## Replication: Interview Questions

1) In transactional replication will we get non clustered indexes by default to Subscribers?
    - No, we need to change the replication settings. by default, this setting will be set FALSE.  we need to enable to TRUE.

